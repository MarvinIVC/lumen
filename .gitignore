# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# cloudflare / opennext
.open-next/
.wrangler/
cloudflare-env.d.ts

# env
.env
.env.local
.env.*.local

# supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# test output
coverage/
playwright-report/
test-results/
blob-report/
.lighthouseci/

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# sentry
.sentryclirc

# typescript incremental build info
*.tsbuildinfo

# Storybook static build
storybook-static/

# Local visual review output (scripts/shoot-marketing.mjs).
screenshots/
.lighthouseci-mobile/

# One-off debugging scripts. `chk.tmp.mjs` — a Playwright snippet for counting paged.js pages —
# was committed by accident in phase-02 and sat in the repo root for three phases, referenced by
# nothing, for every later agent to stop and reason about.
*.tmp.mjs
*.tmp.ts
