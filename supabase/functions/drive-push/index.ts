/**
 * drive-push — exports a finished note to the student's Google Drive.
 *
 * Stub: returns 501 until phase-07 implements it.
 */
import { notImplemented, serve } from '../_shared/response.ts';

const TODO =
  'Refresh the access token, render the note to PDF or Docx server-side, upload into the course folder, return the file link.';

serve((request) => notImplemented(request, 'drive-push', TODO, 'phase-07'));
